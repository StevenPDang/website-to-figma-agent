# Figma Desktop smoke-test record

Status: pending external runtime.

The automated Figma adapter harness passes core-node creation, deterministic ordering, schema validation, and replay/idempotency checks. Figma Desktop was not available during this run, so a real plugin import/export cannot be claimed. Before release, open the plugin in Figma Desktop, connect the localhost session, import `figma-scene.json`, export the resulting frame, and attach the resulting QA report here.
